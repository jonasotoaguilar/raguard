#!/usr/bin/env python3
"""raguard-bootstrap CLI entrypoint (task 4.4).

Reads bootstrap settings exclusively from the environment — secrets never
appear in argv, logs, or output. Exits 0 on success or no-op, 2 on invalid
environment, 1 on runtime failure.
"""

import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.abspath(__file__)), "src"))

from raguard_api.identity.bootstrap import main  # noqa: E402

if __name__ == "__main__":
    raise SystemExit(main())
